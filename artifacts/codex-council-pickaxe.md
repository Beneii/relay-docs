RESULT: Relay becomes the hosted push, session, and control-plane infrastructure for AI/vibe-coded dashboards—a pickaxe that handles auth, notifications, and native wrappers so makers can ship tools without re-building plumbing.
PIVOT: Keep the mobile/web container and webhook push engine, but shift from consumer dashboard launcher to developer platform: expose everything as APIs/SDKs, add dev-focused docs, templates, and automation hooks so Relay powers other people's dashboards instead of being the dashboard.
NEW FEATURES: 1) Relay Projects API—create/manage dashboard apps programmatically with tokens, metadata, and environment secrets; 2) Unified Push + Deep-Link Router—REST + SDK endpoints to fan out notifications, embed payload data, and deep-link into any hosted dashboard/webview; 3) Session Bridge SDK—drop-in JS/React Native kit that embeds Relay auth/device linking into custom dashboards and handles secure reverse tunneling (Tailscale/Tunnel integration); 4) Automation Webhooks + Jobs—queue + retryable webhook invocations, templated messages, and delivery analytics so makers can trigger flows without writing infra; 5) CLI + Templates—`relay init` scaffolds dashboard shells, mobile wrappers, and CI recipes, connecting straight to the API keys.
PRICING: Reframe as a dev tool/SaaS: generous free tier (1 project, 2 devices, 5k pushes/month) to capture hobbyists, then $19 developer plan (5 projects, 50k pushes, log retention) and usage-based scale tiers ($49+ with additional pushes, seats, SLA). Offer add-ons for white-label mobile shells or dedicated tunneling. Billing aligns to per-project/per-push rather than $7.99 consumer subscription.
LANDING PAGE: Headline: "Build the dashboard you want—Relay handles auth, pushes, and native delivery." Value prop: "Relay gives vibe-coders hosted webhooks, device bridges, and native wrappers so your AI-built dashboard has notifications, secure access, and mobile presence in minutes."
COMPETITORS: Pushover/OneSignal (push), Ably/Pusher/Supabase Realtime (event infra), Appsmith/Budibase (internal tool platforms), custom Tailscale/ngrok workflows, DIY Expo shells. Relay must differentiate by bundling push + native wrapper + secure tunnel + developer ergonomics for custom dashboards.
SHORTEST PATH: 1) Productize existing webhook + push pipeline as a documented API with project tokens/CLI; 2) expose saved dashboards/devices via REST + JS SDK so external dashboards can register themselves; 3) polish mobile/web wrapper to accept external dashboard manifests instead of manual entries; 4) add delivery analytics + usage metering to support new pricing; 5) iterate marketing site/docs to pitch "infra for vibe dashboards" before tackling advanced tunneling features.
CONFIDENCE: 0.63
