RESULT: partially-reformed
CONSISTENCY: The hero/features/how-it-works sections in src/App.tsx now pitch “mobile runtime for tools you build with AI” and the pricing page mentions SDK access and 500 notifications on the free tier, so the top-level marketing speaks to builders; the backend + mobile flows also reference SDK concepts (interactive actions, deep links). Still, remnants linger: the MobileDownloadSection and parts of the FAQ keep talking about “saving dashboards” and “no SDK required”, and the product UI still looks like a consumer “save a site” app rather than a developer workspace, so the story is not yet fully coherent across all surfaces.
SDK_VERDICT: packages/sdk/src/relay.ts gives vibe-coders a solid DX (typed config, token validation, retries, timeout, ergonomic Relay.notify) and packages/sdk/src/manifest.ts exposes relayConfig() for manifest scaffolding, but there’s no documentation, code samples, or publishing automation—without docs or discoverability this feels like a hidden utility rather than a flagship SDK.
DIFFERENTIATORS: Interactive notifications in backend/supabase/functions/notify/index.ts now support severity-aware sounds, action callbacks, and deep_link_url routing, and the mobile client actually honors those paths; combined with the native wrapper and auth-as-a-header concept, Relay offers more than generic push pipes like ntfy/Pushover because it bundles webview runtime + deep linking.
GAPS: (1) No guides or dashboard inside the app that show SDK usage, relay.json validation feedback, or action button analytics—developers still have to guess beyond marketing copy. (2) The relay.json ingestion is opportunistic but doesn’t surface validation errors or theme/icon previews, so vibe-coders can’t trust it. (3) There’s no CLI/templates/quickstart (only npm package + landing copy), so getting from “idea” to mobile runtime still requires manual steps and Supabase configuration.
OVERALL: Relay feels meaningfully more “developer infra” than last sprint—there’s a real SDK, interactive push pipeline, and mobile runtime behavior that lines up with the messaging—but the experience remains patchy. Until the documentation, onboarding flows, and UI fully embrace builders (instead of leftover consumer metaphors), the pivot reads as half-finished rather than cosmetic, yet the technical groundwork suggests the team is moving beyond surface copy.
CONFIDENCE: 0.62
