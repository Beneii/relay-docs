RESULT: done
FILES CHANGED:
- src/pages/Docs.tsx — new docs page with sticky sidebar, anchors, copyable code blocks, tables, and all seven sections from the spec.
- src/main.tsx — added lazy-loaded route for /docs.
OUTPUT:
- npm run typecheck (pass)
CONFIDENCE: 0.78
GAPS/ASSUMPTIONS:
- SDK availability note assumes temporary unpublished status; update once package is live on npm.
