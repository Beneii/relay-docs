1. Build a “Notification History & Delivery Logs” view inside the dashboard that shows every notification per app (title, severity, metadata preview, action responses) plus delivery status (stored push tickets, Expo receipt errors) so vibe-coders can debug failed workflows; this leverages existing notifications table and push_tickets data and immediately removes a top trust gap.
2. Ship webhook HMAC signing (shared secret per app, signature header, verification helper in the SDK/docs) to guarantee that actions hitting a developer’s API actually came from Relay, mitigating spoofing risk for destructive approve/kill buttons and required before serious automations will rely on Relay.
3. Publish @relayapp/sdk to npm with CI release automation, README, and versioned docs so builders can install it without cloning the repo; the SDK is now core to the story but inaccessible, and publishing is low effort with outsized impact on adoption and credibility.
