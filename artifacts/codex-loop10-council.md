Product config hardcoding inventory:
1. **Pricing page** (`src/pages/Pricing.tsx`): monthlyPrice `$7.99`, annual `$79`, annualMonthly `$6.58` are hardcoded strings instead of coming from shared config or environment; free plan feature list hardcodes “500 notifications / month” even though `FREE_LIMITS.notificationsPerMonth` holds that value; “Priority support” and “Notification history” labels could be derived from PRO_LIMITS/NOTIFICATION_HISTORY_LIMITS; CTA text “See full history with Pro” references history limits but uses `notificationHistoryLimit` from hooks (OK) while pricing hero text still says “For power users and professional teams.”
2. **Repeated plan limits in app**: various components still hardcode numeric limits instead of shared constants — e.g., `app/app/(tabs)/index.tsx` toast says “Free accounts are limited to 3 dashboards,” `app/src/components/UpgradePrompt.tsx` enumerates `Up to ${PRO_LIMITS.devices}` but “Unlimited dashboards & projects” string is manual; free notifications text in Pricing is literal “500 notifications / month” rather than `${FREE_LIMITS.notificationsPerMonth}`.
3. **Other hardcoded numbers**: The onboarding banner snippet (suggestion) includes no notice of limits; more importantly, notifications search panel uses `NOTIFICATION_HISTORY_LIMITS` but other UI (quiet hours, quotas) embed numeric thresholds in copy; email quota warning logic (not inspected) likely duplicates numbers; the device management UI (Loop3) may display `10` devices from `PRO_LIMITS` but needs verification; no centralized config for plan pricing used across backend (Stripe price IDs) vs frontend—they live only here.
