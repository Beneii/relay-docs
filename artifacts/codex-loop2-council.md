1. **Team + dashboard sharing** — let Pro users invite collaborators (read-only or full access) to specific dashboards with their own device registrations, because many vibe-coder projects quickly become “friend + teammate” efforts and sharing is a clear upgrade trigger beyond raw limits.
2. **Heartbeat / dead-man’s switch monitoring** — allow users to register recurring tasks (cron/agent) with expected intervals and auto-fire a “missed heartbeat” notification if no event arrives in time, solving a painful reliability gap for unattended bots and giving Relay a unique must-pay feature.
3. **Turbo onboarding + relay.json validation feedback** — add a first-run wizard (sample dashboard + test notification) and surface relay.json errors inline in edit-app; current TTV is too long and silent failures in manifest fetch leave new users confused, so tightening this flow reduces churn before they ever try paying features.
