1. **Team + dashboard sharing with per-user device access** — let Pro subscribers invite collaborators to specific dashboards and manage their notification channels/devices; this is a clear upgrade trigger and enables small AI teams to adopt Relay as shared ops tooling rather than a solo sandbox.
2. **Rate-limited, signed Vercel API surface** — lock down /api/heartbeat (and future webhooks) with HMAC or API key verification plus per-IP rate limiting; the current unauthenticated endpoints are a security liability and could let attackers spoof heartbeats or DOS the cron worker, so tightening this now prevents reputational damage.
3. **relay.json + edit-app validation feedback** — surface manifest errors inline (show exact field issues when auto-fetch fails) and add a “Preview” of icon/tabs; fixing this removes a top confusion point for first-time builders and reduces support churn when manifests silently fail.
