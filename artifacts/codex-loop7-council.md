1. **Team & dashboard sharing (Pro-only)** — implement shareable dashboards with role-based access (viewer/editor), seat billing, and per-user notification channels; this unlocks multi-user adoption, a clear paid upsell, and makes Relay sticky inside small AI teams.
2. **Publish @relayapp/sdk to npm with CLI onboarding** — finish the publish pipeline, add README/examples, and bundle a `relay init` CLI that scaffolds relay.json + sample scripts; this slashes time-to-value for vibe-coders and ensures the public story matches reality.
3. **relay.json validation + preview in edit-app** — surface manifest errors inline (field-level), show icon/tab preview, and let users fix issues without guessing; this removes a major onboarding papercut and makes Relay’s runtime automation (auto-tabs, themes) reliable at scale.
