Frontend plan for team sharing:
1. **InviteMemberModal / inline section** — in `src/features/dashboard/modals.tsx` or a new `InviteMemberModal.tsx`, expose email + role dropdown (viewer/editor), call `/api/invite-member`, and list pending invites (status, resend, cancel). Trigger lives in `DashboardListSection` (per-app) and in edit modal. Free-plan users hitting “Invite” should see an upgrade modal (reuse existing billing CTA).
2. **MembersSection component** — add `src/features/dashboard/MembersSection.tsx` that renders accepted members and pending invites with role tags, owner badge, and “Remove” / “Resend invite” actions. Include it below each dashboard card (inside `DashboardListSection`) for owners. Uses data from `dashboard_members` query added to `useDashboardPage.ts` + `types.ts`.
3. **Dashboard list badges + ownership context** — in `DashboardListSection.tsx`, show a “Shared” pill when `dashboard.is_shared` is true and display owner name/email for dashboards where `dashboard.owner_id !== user.id`. Change action buttons (delete/invite) visibility based on role.
