Plan for mobile notification history inbox:
1. **API hook + store** — add `app/src/hooks/useNotificationsHistory.ts` that uses Supabase to fetch `notifications` with pagination (`limit` + `lt('created_at', cursor)`), storing `data`, `loadMore`, `refresh` state; leverage `useInfiniteQuery` from TanStack or manual state in `zustand` store `notificationHistoryStore` under `app/src/stores`.
2. **Inbox screen** — create `app/app/notifications/history.tsx` (or extend existing notifications tab) with a FlatList showing items: severity badge, channel tag, title/body preview, relative timestamp via `date-fns`/custom helper. Use `refreshControl` for pull-to-refresh and `onEndReached` to request next page. When tapping a row, expand to show full body + metadata (maybe a modal or inline expansion).
3. **Navigation wiring** — update `app/app/(tabs)/notifications.tsx` to include a “History” screen or stack (e.g., new route `app/app/(tabs)/notifications/history.tsx`) and add link from current notifications view; ensure Supabase call filters by `user_id` and order desc. Update `app/src/stores/authStore.ts` if token is needed for Supabase JS call.
