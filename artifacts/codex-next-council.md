1. **Publish & document @relayapp/sdk + CLI scaffolding** — get the SDK on npm with a proper README, versioned docs, and a `relay init` CLI that scaffolds relay.json + sample scripts; this slashes time-to-first-notification, aligns marketing with reality, and removes the current friction where vibe-coders must copy-paste from source.
2. **Team collaboration UX (share dashboards + member permissions)** — expand the backend work into polished UI: per-app member lists, invite management, role-based controls, and visibility cues in dashboard cards; sharing is the clearest upgrade trigger and keeps teams anchored in Relay daily.
3. **Notification analytics & delivery assurance** — add a dashboard view for delivery stats (sent/delivered/failed per channel, action click-through, heartbeat misses) with exportable logs; builders need trustworthy observability to rely on Relay for critical agents, and this differentiates us from simple push bridges.
