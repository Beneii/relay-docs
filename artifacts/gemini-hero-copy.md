# Hero Copy & Landing Page Audit

## 1. 404 NotFound Route
- Verified that `src/pages/NotFound.tsx` exists and is correctly integrated into the router in `src/main.tsx`.
- The design is minimal, using `bg-bg`, `text-text`, and the blue accent (`text-accent`).

## 2. Hero Section Improvements
- **Headline:** Changed to "Push notifications for your dashboards and scripts." to more directly address the primary value proposition.
- **Subtext:** Refined to "Bridge the gap between your critical infrastructure and your phone. Trigger instant mobile alerts from any script, server, or internal tool with a simple webhook."
- **Tone:** Professional, direct, and free of emojis.

## 3. How It Works Section
- Confirmed the section exists.
- Added a "Try it now" code example block immediately following the steps.
- The example includes a clean `curl` command showing the webhook integration style.

## 4. Pricing Updates
- Refined the "Free" plan visual structure.
- Added explicit indicators for non-included features (Up to 10 devices, Notification history, Priority support) using `X` icons and reduced opacity.
